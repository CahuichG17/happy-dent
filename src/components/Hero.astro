---
---

<section class="hero" id="inicio">
  <div class="hero-bg-shapes">
    <div class="shape shape-1"></div>
    <div class="shape shape-2"></div>
    <div class="shape shape-3"></div>
    <div class="shape shape-4"></div>
    <div class="shape shape-5"></div>
  </div>

  <div class="container hero-container">
    <div class="hero-content">
      <div class="hero-badge" data-animate="fade-up">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
        +15 años de experiencia
      </div>

      <h1 class="hero-title" data-animate="fade-up">
        Tu sonrisa,<br/>
        nuestra <span class="highlight">pasión</span>
      </h1>

      <p class="hero-description" data-animate="fade-up">
        En Happy Dent creemos que cada sonrisa merece brillar. Ofrecemos tratamientos 
        odontológicos de vanguardia en un ambiente cálido y profesional, para que tu 
        experiencia dental sea siempre positiva.
      </p>

      <div class="hero-ctas" data-animate="fade-up">
        <a href="https://wa.me/521234567890?text=Hola%20Happy%20Dent%2C%20me%20gustaría%20agendar%20una%20cita" target="_blank" rel="noopener" class="btn btn-primary">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/></svg>
          Agenda tu Cita
        </a>
        <a href="#servicios" class="btn btn-secondary">
          Conoce Nuestros Servicios
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 17l9.2-9.2M17 17V7H7"/></svg>
        </a>
      </div>

      <div class="hero-stats" data-animate="fade-up">
        <div class="stat">
          <span class="stat-number" data-count="5000">0</span>
          <span class="stat-plus">+</span>
          <span class="stat-label">Pacientes Felices</span>
        </div>
        <div class="stat-divider"></div>
        <div class="stat">
          <span class="stat-number" data-count="15">0</span>
          <span class="stat-plus">+</span>
          <span class="stat-label">Años de Experiencia</span>
        </div>
        <div class="stat-divider"></div>
        <div class="stat">
          <span class="stat-number" data-count="20">0</span>
          <span class="stat-plus">+</span>
          <span class="stat-label">Tratamientos</span>
        </div>
      </div>
    </div>

    <div class="hero-visual" data-animate="fade-left">
      <div class="hero-image-wrapper">
        <div class="hero-image-bg"></div>
        <div class="hero-image-content">
          <img src="/happy-dent-icon.svg" alt="Happy Dent Logo" class="hero-tooth">
        </div>

        <!-- Floating cards -->
        <div class="floating-card card-1">
          <div class="floating-card-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#FFB1D7" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M8 14s1.5 2 4 2 4-2 4-2"/><line x1="9" y1="9" x2="9.01" y2="9"/><line x1="15" y1="9" x2="15.01" y2="9"/></svg>
          </div>
          <div class="floating-card-text">
            <strong>Sonrisas</strong>
            <span>+5,000 pacientes</span>
          </div>
        </div>

        <div class="floating-card card-2">
          <div class="floating-card-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="#FFB1D7" stroke="none"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
          </div>
          <div class="floating-card-text">
            <strong>Calificación</strong>
            <span>5.0 / 5.0</span>
          </div>
        </div>

        <div class="floating-card card-3">
          <div class="floating-card-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#D8B9EB" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5c-2 0-4 2-4 4s2 6 4 10c2-4 4-8 4-10s-2-4-4-4z"/><path d="M8 12c-1-1-2.5 0-1.5 3s3.5 5 5.5 5 5-2 5.5-5 .5-4-1.5-3"/></svg>
          </div>
          <div class="floating-card-text">
            <strong>Tecnología</strong>
            <span>De vanguardia</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="scroll-indicator" data-animate="fade-up">
    <div class="scroll-mouse">
      <div class="scroll-wheel"></div>
    </div>
    <span>Desliza hacia abajo</span>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
    overflow: hidden;
    background: var(--gradient-hero);
    padding-top: 5rem;
  }

  .hero-bg-shapes {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .shape {
    position: absolute;
    border-radius: 50%;
    opacity: 0.08;
  }

  .shape-1 {
    width: 600px;
    height: 600px;
    background: var(--pink);
    top: -200px;
    right: -200px;
    animation: float 20s ease-in-out infinite;
  }

  .shape-2 {
    width: 400px;
    height: 400px;
    background: var(--lavender);
    bottom: -100px;
    left: -100px;
    animation: float 15s ease-in-out infinite reverse;
  }

  .shape-3 {
    width: 200px;
    height: 200px;
    background: var(--pink);
    top: 50%;
    left: 30%;
    animation: float 18s ease-in-out infinite 2s;
  }

  .shape-4 {
    width: 150px;
    height: 150px;
    background: var(--lavender);
    top: 20%;
    right: 30%;
    animation: float 12s ease-in-out infinite 5s;
  }

  .shape-5 {
    width: 100px;
    height: 100px;
    background: var(--pink-medium);
    bottom: 30%;
    right: 10%;
    animation: float 16s ease-in-out infinite 3s;
  }

  @keyframes float {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(30px, -30px) scale(1.05); }
    66% { transform: translate(-20px, 20px) scale(0.95); }
  }

  .hero-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 2;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(255, 177, 215, 0.12);
    border: 1px solid rgba(255, 177, 215, 0.2);
    padding: 0.6rem 1.25rem;
    border-radius: var(--radius-full);
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--pink-dark);
    margin-bottom: 1.5rem;
    backdrop-filter: blur(10px);
  }

  .hero-badge svg {
    color: var(--pink);
    fill: var(--pink);
    stroke: none;
  }

  .hero-title {
    font-size: clamp(2.75rem, 5.5vw, 4.5rem);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--text-dark);
  }

  .hero-title .highlight {
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    position: relative;
  }

  .hero-title .highlight::after {
    content: '';
    position: absolute;
    bottom: 2px;
    left: 0;
    right: 0;
    height: 3px;
    background: var(--gradient-primary);
    border-radius: 2px;
    opacity: 0.5;
  }

  .hero-description {
    font-size: clamp(1rem, 1.3vw, 1.15rem);
    color: var(--text-light);
    max-width: 540px;
    margin-bottom: 2.5rem;
    line-height: 1.8;
  }

  .hero-ctas {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 3rem;
  }

  .hero-stats {
    display: flex;
    align-items: center;
    gap: 2rem;
    padding: 1.5rem 2rem;
    background: rgba(255, 255, 255, 0.6);
    backdrop-filter: blur(15px);
    border-radius: var(--radius-lg);
    border: 1px solid rgba(255, 177, 215, 0.15);
    box-shadow: var(--shadow-sm);
    max-width: fit-content;
  }

  .stat {
    text-align: center;
    position: relative;
  }

  .stat-number {
    font-family: 'Cormorant Garamond', serif;
    font-size: 2rem;
    font-weight: 700;
    color: var(--pink-dark);
  }

  .stat-plus {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--lavender);
  }

  .stat-label {
    display: block;
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--text-light);
    margin-top: 0.25rem;
  }

  .stat-divider {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, transparent, var(--pink), transparent);
  }

  /* Hero Visual */
  .hero-visual {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-image-wrapper {
    position: relative;
    width: 100%;
    max-width: 500px;
    aspect-ratio: 1;
  }

  .hero-image-bg {
    position: absolute;
    inset: 10%;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(255, 177, 215, 0.15) 0%, rgba(216, 185, 235, 0.1) 50%, transparent 70%);
    animation: pulse-soft 4s ease-in-out infinite;
  }

  @keyframes pulse-soft {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.05); opacity: 0.8; }
  }

  .hero-image-content {
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-tooth {
    width: 85%;
    height: 85%;
    object-fit: contain;
    animation: tooth-float 6s ease-in-out infinite;
  }

  @keyframes tooth-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-15px); }
  }

  .sparkle-main {
    animation: sparkle 3s ease-in-out infinite;
  }

  .sparkle-cross {
    animation: sparkle 3s ease-in-out infinite;
  }

  @keyframes sparkle {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(0.8); }
  }

  .sparkle-sm {
    animation: sparkle-float 4s ease-in-out infinite;
  }

  .sparkle-1 { animation-delay: 0s; }
  .sparkle-2 { animation-delay: 1s; }
  .sparkle-3 { animation-delay: 2s; }
  .sparkle-4 { animation-delay: 3s; }

  @keyframes sparkle-float {
    0%, 100% { opacity: 0.3; transform: translateY(0); }
    50% { opacity: 0.7; transform: translateY(-10px); }
  }

  /* Floating Cards */
  .floating-card {
    position: absolute;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.85rem 1.25rem;
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(15px);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-md);
    border: 1px solid rgba(255, 177, 215, 0.15);
    animation: card-float 5s ease-in-out infinite;
    z-index: 3;
  }

  .floating-card-icon {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(255, 177, 215, 0.1), rgba(216, 185, 235, 0.1));
    border-radius: var(--radius-sm);
    flex-shrink: 0;
  }

  .floating-card-text {
    display: flex;
    flex-direction: column;
  }

  .floating-card-text strong {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--text-dark);
  }

  .floating-card-text span {
    font-size: 0.75rem;
    color: var(--text-light);
  }

  .card-1 {
    top: 10%;
    right: 0;
    animation-delay: 0s;
  }

  .card-2 {
    bottom: 25%;
    left: -5%;
    animation-delay: 1.5s;
  }

  .card-3 {
    bottom: 5%;
    right: 5%;
    animation-delay: 3s;
  }

  @keyframes card-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  /* Scroll Indicator */
  .scroll-indicator {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    padding: 2rem 0;
    position: relative;
    z-index: 2;
  }

  .scroll-mouse {
    width: 26px;
    height: 40px;
    border: 2px solid var(--pink);
    border-radius: 13px;
    position: relative;
    opacity: 0.6;
  }

  .scroll-wheel {
    width: 4px;
    height: 8px;
    background: var(--pink);
    border-radius: 2px;
    position: absolute;
    top: 8px;
    left: 50%;
    transform: translateX(-50%);
    animation: scroll-anim 2s ease-in-out infinite;
  }

  @keyframes scroll-anim {
    0% { opacity: 1; transform: translateX(-50%) translateY(0); }
    100% { opacity: 0; transform: translateX(-50%) translateY(12px); }
  }

  .scroll-indicator span {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 0.75rem;
    color: var(--text-muted);
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  /* Responsive */
  @media (max-width: 900px) {
    .hero-container {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 2rem;
    }

    .hero-description {
      margin-inline: auto;
    }

    .hero-ctas {
      justify-content: center;
    }

    .hero-stats {
      margin-inline: auto;
      gap: 1.5rem;
      padding: 1.25rem 1.5rem;
    }

    .hero-visual {
      order: -1;
    }

    .hero-image-wrapper {
      max-width: 320px;
    }

    .floating-card {
      display: none;
    }

    .stat-number {
      font-size: 1.5rem;
    }
  }
</style>
